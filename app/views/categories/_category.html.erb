<div class="category">
	<%= link_to image_tag(category.title_image.url(:normal), :alt => category.title), category %>

	<% category.photos.each do |p| %>
		<%= link_to photo_image(p, :small), p %>
	<% end %>
</div>
